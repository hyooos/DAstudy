# Week 03 — 변수와 척도, 기술통계, 심슨의 역설

| 날짜 | 데이터 | 발제자 | PR 마감 |
| --- | --- | --- | --- |
| 2026.09.27 (일) | 🏦 Bank Marketing | @ | 09.30 (수) 23:59 |

## 🎯 사용 데이터: 🏦 Bank Marketing

**왜 이 데이터?** 행이 날짜순인데 연도 열이 없어서 "데이터에 없는 변수가 결론을 뒤집는" 심슨의 역설을 직접 복원해볼 수 있음. 평균·중앙값이 반대 방향인 `age`, 음수·극단값이 있는 `balance`, 코드값 -1이 섞인 `pdays`까지 대표값의 함정이 한 데이터에 다 모여 있음.

**유의깊게 봤으면 하는 것**

- 척도는 판정 결과보다 **판정 근거**가 중요
- "숫자 하나만" 요청이어도 분포를 먼저 보고, 어떤 대표값이 "전형적인 고객"을 말하는지 고르기
- 전체 비교와 하위 집단(연도) 비교의 방향이 달라지는지

---

> 📩 "가입한 고객과 안 한 고객이 어떻게 다른지 한 장짜리 표로 정리해 주세요. 임원 보고용이라 숫자 하나씩만요." — 마케팅팀장

**케이스 질문:** 각 변수를 대표값 하나로 보고한다면 무엇을 써야 하나? 그 표는 믿을 만한가?

**AI에게 먼저:** "가입자와 미가입자의 특성을 기술통계로 비교해줘."

## 필수 과제

- [ ] 17개 변수의 척도(명목·서열·등간·비율)를 판정해 변수 사전 채우기 → `docs/data_dictionary/bank.md`
- [ ] `age`, `balance`, `duration`, `campaign`의 가입자/미가입자별 평균·중앙값 비교표 → **평균과 중앙값이 반대 방향을 가리키는 변수가 있는지** 찾기
- [ ] `balance`의 왜도, 음수·0 개수, 최빈값 확인 → 평균이 "전형적인 고객"을 대표하는지
- [ ] `balance`에서 평균 ±1·2·3σ 안에 들어오는 비율 → 경험법칙(68-95-99.7)과 비교
- [ ] `pdays`의 평균을 -1 포함 / 제외로 각각 계산

## 심화 과제

- [ ] 행 순서와 `month`로 **연도를 복원**하기 (월이 앞으로 돌아가면 해가 바뀐 것) → 연도별 건수와 가입률
- [ ] 직업(technician vs admin.) 또는 이전 캠페인 결과(unknown vs failure)의 가입률을 전체로 비교한 뒤 **연도별로 나눠서** 다시 비교

## 토론 질문

- 임원 보고용 표에 평균을 넣을까, 중앙값을 넣을까? 둘 다 넣는다면 어떻게 설명할까?
- 데이터에 없는 변수(연도)가 결론을 뒤집을 수 있다면, 우리는 그걸 어떻게 미리 의심할 수 있을까?

## 이번 주에 쌓을 것

- `docs/data_dictionary/bank.md` 척도 완성
- `common/load_data.py`의 `load_bank()` 확정
- `docs/decisions.md` **D-003 [공통]** "치우친 수치 변수는 평균과 중앙값을 함께 보고"
- `docs/audit_checklist.md` "척도에 맞지 않는 통계량을 쓰지 않았는가?", "치우친 변수를 평균만으로 요약하지 않았는가?"
- **Git 연습:** 첫 정식 PR과 리뷰, 리뷰 반영 후 재push

## 🔍 진행 가이드 & 유의할 점

### 도입 — UCBAdmissions (15분)

1. 전체 합격률(남 약 45% vs 여 약 30%)만 먼저 보여주고 "차별인가?" 투표
2. 학과별 합격률 공개 → 여러 학과에서 여성 합격률이 비슷하거나 더 높음
3. 학과별 지원자 성비를 같이 보여주며 여성이 경쟁률 높은 학과에 많이 지원했다는 게 원인임을 확인
4. "bank에서 '학과' 역할을 할 숨은 변수는?" 질문으로 본 분석 시작 (→ 연도)

### bank에서 유의해서 볼 것

- 척도 판정은 결과보다 **근거**가 중요. `day`, `month`는 숫자처럼 보여도 크기 비교가 무의미(월은 순환), `education`은 서열이지만 unknown의 위치를 정해야 함, `pdays`는 -1 때문에 그대로 비율척도로 보면 안 됨
- 평균·중앙값 반대 방향: `age`가 대표 사례. 반대라는 걸 찾는 데서 끝내지 말고 연령대별 가입률(U자) 그래프로 **왜** 그런지까지 설명해야 완성
- `balance`: 평균이 중앙값의 약 3배, 최빈값은 0. 음수·0 개수를 같이 보고 "전형적인 고객"을 무엇으로 보고할지 근거 쓰기
- 경험법칙: `balance`의 ±1·2·3σ 비율이 68-95-99.7에서 크게 벗어남 → 어느 방향으로 벗어나는지 해석하면 "정규가 아니다"의 수치 근거가 됨
- `pdays` 평균: -1 포함 약 40일 vs 제외 약 224일 → 코드값 하나가 대표값을 얼마나 망가뜨리는지
- 연도 복원: `month`를 1~12로 매핑 → `month_num.diff() < 0`인 지점마다 연도 +1 (2008년은 5월 시작). 결과가 2008~2010 세 해로 나오는지 검증 (더 많이 나오면 정렬이 깨진 구간이 있다는 뜻)
- 심슨의 역설은 모든 조합에서 뒤집히지 않음. 뒤집히지 않은 경우도 결과로 기록하기

### AI 검증 포인트

`describe()` 평균만 비교하는지, `day` 평균처럼 척도에 안 맞는 통계량을 내는지, `pdays` -1을 포함해 계산하는지, 중앙값을 빠뜨리는지

### 발제자 준비

임원 보고용 한 장 표를 두 버전(평균만 / 평균+중앙값)으로 만들어 토론 질문에 활용

## 📦 보조 데이터 (선택)

| 데이터 | 링크 | 해볼 것 |
| --- | --- | --- |
| UC버클리 입학 (UCBAdmissions) | Rdatasets · `sm.datasets.get_rdataset("UCBAdmissions").data` | 심슨의 역설 고전 사례. 전체 합격률과 학과별 합격률, 학과별 여성 지원 비중을 한 표에 |
| Palmer Penguins | GitHub · `sns.load_dataset("penguins")` | 부리 길이·깊이 상관이 전체로는 음수, 종별로는 양수 → 연속형 심슨의 역설 |
| Adult (미국 인구조사) | UCI | `capital-gain`은 대부분 0이고 일부만 큼 → 평균·중앙값·최빈값이 각각 무엇을 말하는지. `education`(서열)과 `education-num`(숫자)의 차이 |
| 가계금융복지조사 (통계청) | KOSIS | 실제 한국 가구소득의 평균과 중위값을 찾아 비교 → 보도자료가 어떤 대표값을 헤드라인에 썼는지 |

---

## 📝 발제자 작성

### 교재 범위
-

### 개념 정리
<!-- D-7 ~ D-2 -->

### 모임 후 해설
- **질문:**
- **함정:**
- **해설:**
